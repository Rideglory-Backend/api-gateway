{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug API Gateway",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run start -- --debug 9231 --watch",
      "cwd": "${workspaceFolder}"
    }
  ]
}
